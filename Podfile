# Uncomment the next line to define a global platform for your project platform :ios, '10.0'

target 'BitGrayTrial' do
  use_frameworks!

  # Pods for BitGrayTrial
  pod 'Alamofire', '~> 4.0'
  pod 'ObjectMapper', '~> 2.0'
  pod 'AlamofireObjectMapper', '~> 4.0'

  target 'BitGrayTrialTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BitGrayTrialUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
